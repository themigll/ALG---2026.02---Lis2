#include <stdio.h>
#include <stdlib.h>

int main(void) {

    int i, soma = 2;

    for (i = 1; i < 7; i++) {
        soma += (4 + 1);
    }

    printf("A soma das pecas com 4 e: %d\n", soma);

    system("pause");
    return 0;
}
